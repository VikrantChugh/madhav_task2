name: workflow_01
on:
  workflow_dispatch:
     inputs:
      workflow_02:
        description: 'ًWorkflow 2 which will be triggered'
        required: true
        default: 'workflow_02'

      workflow2_github_account:
        description: 'GitHub Account Owner'
        required: true
        default: 'VikrantChugh'

      workflow2_repo_github:
        description: 'repo-name'
        required: true
        default: 'madhav_task2.0' 

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - name: Test
        run: |
          curl -X POST https://api.github.com/repos/:userName/:repoName/dispatches \
          -H 'Accept: application/vnd.github.everest-preview+json' \
          -u ${{ secrets.TOKEN_GITHUB }} \
          --data '{"event_type": "Trigger Workflow", "client_payload": { "repository": "'"$madhav_task2.0"'" }}'
      - uses: actions/checkout@v3

  
         

